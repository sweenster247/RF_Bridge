# RF Bridge v1.9.5.14

## Changed
- Cleaned tinySA device labels so raw console echoes like `version` and `ch>` are not shown in the UI.
- Simplified the connected-state action row to show a single Disconnect button.
- Removed the dark status-dot block in the connection card.
