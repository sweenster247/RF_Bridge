[project]
name = "rf-bridge"
version = "1.9.5.13"
description = "tinySA to WWB bridge and live RF visualization utility for macOS"
requires-python = ">=3.11"
dependencies = [
    "pyserial",
    "PySide6",
    "pyqtgraph",
]

[tool.rf-bridge]
app-name = "RF Bridge"
