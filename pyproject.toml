[project]
name = "rf-bridge"
version = "1.9.2"
description = "tinySA to Wireless Workbench RF scan bridge"
requires-python = ">=3.10"
dependencies = [
    "pyserial",
    "PySide6",
    "pyqtgraph",
    "pyinstaller",
]

[project.scripts]
rf-bridge = "rf_bridge.app:main"
