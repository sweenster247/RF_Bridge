# RF Bridge v1.9.4.4

Patch release for the v1.9.4 series.

## Fixed
- Fixed app launch failure caused by missing Qt widget imports in the overlay panel.
- Restored `python3 rf-bridge.py --ui` startup behavior.
- Keeps the v1.9.4.3 shutdown handling changes intact.

## Build

```bash
./build_release.sh
```
