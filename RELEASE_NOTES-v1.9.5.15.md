# RF Bridge v1.9.5.15

## Added
- Added `Demo Mode — simulated RF scan` to the device dropdown.
- Demo Mode updates the graph and RF summary without requiring a tinySA.

## Safety
- Demo Mode does not write `latest_scan.csv`.
- Demo Mode does not create timestamped CSV captures.
- Demo Mode does not touch `wwb_scans`.
